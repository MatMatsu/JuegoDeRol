package juegoderolv3;

public class Monje{
    /*
    //ATRIBUTO
    int danio;
    String descAtEsUno = "<html>Iluminacion: Recupera 20 de vida y hace daño de entre 0 y " + super.getMinPower() + ".</html>";
    String descAtEsDos = "<html>Luz Sagrada: Recupera 30 de vida y hace daño de entre 0 y " + super.getMinPower() + ".</html>";
    //CONSTRUCTOR
    public Monje () {
        
    }
    public Monje (String nombre, String clase) {
        super(nombre, clase);
        this.descAtEsUno = descAtEsUno;
        this.descAtEsDos = descAtEsDos;
    }
    //GETTER
    public String getDescAtEsUno() {
        return descAtEsUno;
    }
    public String getDescAtEsDos() {
        return descAtEsDos;
    }
    //METODO
    public int ataqueEspecialUno () {
        danio = super.golpear() - super.getMinPower();
        super.setEnergia(super.getEnergia()-20);
        if (super.getVida() >= 80) {
            super.setVida(100);
        } else {
            super.setVida(super.getVida()+20);
        }
        return danio;
    }
    public int ataqueEspecialDos () {
        danio = super.golpear() - super.getMinPower();
        super.setEnergia(super.getEnergia()-40);
        if (super.getVida() >= 70) {
            super.setVida(100);
        } else {
            super.setVida(super.getVida()+30);
        }
        return danio;
    }
    */
}
